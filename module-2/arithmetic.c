#include <stdio.h>

int main()
{
    int a = 15, b = 4;
    // int c = a + b;
    // int c = b - a;
    // int c = a * b;
    // float c = (a * 1.0) / b;
    int c = a % b;
    printf("%d", c);
    return 0;
}