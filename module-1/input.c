#include <stdio.h>

int main()
{
    // int a, b, c;
    // scanf("%d %d %d", &a, &b, &c);
    // printf("%d %d %d", a, b, c);

    // int a;
    // float b;
    // char c;
    // scanf("%d %f %c", &a, &b, &c);
    // printf("%d %0.2f %c", a, b, c);

    // int a, b, c;
    // char p;
    // scanf("%d%c %d%c %d%c", &a, &p, &b, &p, &c, &p);
    // printf("%d%% %d%% %d%%", a, b, c);

    int a, b, c;
    scanf("%d%% %d%% %d%%", &a, &b, &c);
    printf("%d%% %d%% %d%%", a, b, c);
    // scanf("%da %db %dc", &a, &b, &c);
    // printf("%da %db %dc", a, b, c);
    return 0;
}